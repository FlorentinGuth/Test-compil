with Ada.Text_IO; use Ada.Text_IO;

procedure fonctions_args_1 is

   	function r (a: integer) return integer is
   		type a is record x:integer; end record;
   		begin 
		return 1;
		end;

begin New_Line; End;
