with Ada.Text_IO; use Ada.Text_IO;

procedure fonctions_args_4 is
   	function f (f: integer) return integer is
	type f is record x : integer; end record;
   	begin return 1;	end;

begin New_Line; End;
