with Ada.Text_IO; use Ada.Text_IO;
-- les arguments sont au meme niveau que les déclarations qui suivent.

procedure fonctions_args_4 is
   	function f (a: integer) return integer is
	type a is record x : integer; end record;
   	begin return 1;	end;

begin New_Line; End;
